/**
 * Grain de papier des designs qui representent un objet imprime (Signature,
 * invitations). Bruit fractal SVG en surimpression, jamais une image.
 */
export { GRAIN } from "@/components/invitations/stationery";
